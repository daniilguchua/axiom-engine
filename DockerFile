# 1. Base Image: Use a lightweight version of Python
FROM python:3.9-slim

# 2. Work Directory: Where our code lives inside the box
WORKDIR /app

# 3. Dependencies: Copy requirements first to leverage Docker caching
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 4. Copy Code: Move your actual source code into the box
COPY . .

# 5. Network: Allow the world to see port 5000
EXPOSE 5000

# 6. Launch: How to start the app
CMD ["python", "app.py"]